@model List<AttendenceWithCourses>

<style>
    body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background-color: #f9f9f9;
        margin: 0;
        padding: 20px;
    }

    .page-title {
        text-align: center;
        margin-bottom: 30px;
        font-size: 2.5em;
        color: #007bff;
        font-weight: bold;
        text-transform: uppercase;
        position: relative;
    }

        .page-title::after {
            content: '';
            display: block;
            width: 100px;
            height: 5px;
            background-color: #007bff;
            margin: 10px auto 0;
        }

    .card-container {
        display: flex;
        flex-wrap: wrap;
        gap: 20px;
        justify-content: center;
        padding: 20px;
    }

    .card {
        width: 200px;
        border: 1px solid #ddd;
        border-radius: 10px;
        overflow: hidden;
        text-align: center;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        background-color: #fff;
        transition: background-color 0.3s, transform 0.3s, box-shadow 0.3s;
    }

        .card:hover {
            transform: scale(1.05);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            background-color: #FFF380; /* لون أزرق خفيف */
        }

        .card img {
            width: contain;
            height: 100%;
            object-fit: cover;
            border-bottom: 2px solid #007bff;
        }

        .card h4 {
            margin: 10px 0;
            font-size: 1.2em;
            color: #007bff;
        }

        .card p {
            margin: 5px 0;
            font-size: 0.9em;
            color: #555;
        }

        .card a {
            display: inline-block;
            padding: 8px 12px;
            background-color: #007bff;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-size: 0.9em;
            margin-top: 10px;
            transition: background-color 0.3s, transform 0.3s;
        }

            .card a:hover {
                background-color: #0056b3;
                transform: translateY(-3px);
            }
</style>

<h1 class="page-title"> @ViewBag.Student.Name  </h1>

<div class="card-container">
    @foreach (var item in Model)
    {
        <div class="card">
            <img src="/Image/@item.Image" alt="@item.Name" />
            <div>
                <h4>@item.Name</h4> 
                <p>Doctor : @item.doc_Name</p>
                <p> lectures attend : @item.Attendence_count</p>
                <p> lectures given : @item.Attendence_max</p>
                <a href="http://localhost:56652/Course/CoursePage/@item.Course_id">View Course</a>
            </div>
        </div>
    }
</div>

